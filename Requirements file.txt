# ================================================================
#  PROJECT   : BigMart Sales Prediction
#  AUTHOR    : Sejal Salvi
#  FILE      : requirements.txt
#  PURPOSE   : All Python dependencies required to run this project
#              (ML model + Flask web application)
# ================================================================


# ── 1. DATA HANDLING ─────────────────────────────────────────────
# pandas  : Data loading, cleaning, feature engineering, and EDA
pandas==2.0.3

# numpy   : Numerical operations, array handling, RMSE calculation
numpy==1.24.3


# ── 2. MACHINE LEARNING ──────────────────────────────────────────
# scikit-learn : Train/test split, Label Encoding,
#                RandomForestRegressor, and evaluation metrics
#                (MAE, RMSE, R² Score)
scikit-learn==1.3.0


# ── 3. MODEL PERSISTENCE ─────────────────────────────────────────
# pickle  : Built-in Python library — used to save and load the
#           trained model as model.pkl (no install needed)
# pickle-mixin : Compatibility wrapper for pickle serialization
pickle-mixin==1.0.2


# ── 4. WEB APPLICATION (Flask) ───────────────────────────────────
# flask       : Lightweight web framework used to build the
#               BigMart prediction web interface
flask==2.3.3

# Werkzeug    : WSGI utility library required by Flask internally
Werkzeug==2.3.7

# Jinja2      : HTML templating engine used by Flask to render
#               the prediction result on the web page
Jinja2==3.1.2

# gunicorn    : Production-grade WSGI server to deploy the
#               Flask app (used when hosting on cloud/server)
gunicorn==21.2.0


# ── 5. UTILITY ───────────────────────────────────────────────────
# itsdangerous : Secure data signing used by Flask sessions
itsdangerous==2.1.2

# click        : Command-line interface toolkit used by Flask
click==8.1.7


# ================================================================
#  HOW TO INSTALL:
#  Run the following command in your terminal / command prompt:
#
#       pip install -r requirements.txt
#
#  Make sure Python 3.8 or above is installed.
# ================================================================